# antislop audit-001 — 2026-09-06

Scope: `fast-9router/dashboard` all screens vs `fast-9router/DESIGN.md`.
Mode: AFTER (audit). No edits applied. Priorities: Hard Gate = HIGH, Purpose-Gate = MEDIUM, Quality Locks = LOW.

## Findings

1. **HIGH — R-02 em dash in UI text.** `login.tsx:88`, `settings.tsx:54` ("123456 — change it"), `tools.tsx:100,103,133,137,142,147,149,371,410` (CLI descriptions, "— create one first", empty-cell "—"). DESIGN.md only documents; UI text must use `,`/`.`/`:`.

2. **HIGH — R-25 contrast: `.text-subtle #a3a3a3` on `#fff` = 2.85:1** (below 4.5:1). Used for placeholders, `.back-link`, provider-link chevrons, date-control icons. Must move to `#737373` (`{colors.body}`) minimum; reserve `#a3a3a3` for decorative-only per DESIGN.md.

3. **MEDIUM — R-29 palette breach in Usage topology.** Hardcoded glow palette: `#22d3ee` cyan halo/edges + `drop-shadow` glows, `#fde047` yellow badge/active ring + `box-shadow 0 0 16/22px` router glow, `#f8fafc`, `#67e8f9`. DESIGN.md: "Don't introduce gradients, drop shadows"; palette is black/white/gray only.

4. **MEDIUM — R-29 palette breach in Usage chart lines.** `.chart-line-tokens #6366f1` indigo, `.chart-line-cost #f59e0b` amber. Not in system.

5. **MEDIUM — R-01/R-12 shadows+glow as decoration.** `.topology-router.is-active` glow animation, `.edge-core`/`.edge-halo` SVG drop-shadow filters, `box-shadow 0 0 22px rgba(34,211,238,.42)`. DESIGN.md: zero shadow elevation.

6. **MEDIUM — R-06 uppercase tracked labels (7 sites).** `.data-table th`, `.eyebrow`, usage mobile `td:before`, recent-columns span, `.detail-drawer dt`, `.cli-guide-code-head` — `text-transform:uppercase` + `letter-spacing:.035–.05em`. DESIGN.md: "almost no letter-spacing variation", no uppercase tier.

7. **MEDIUM — R-06 off-token font weights.** `font-weight:650` ×12, `700` ×2 in screens.css; 620-era leftovers. DESIGN.md weights are only 400/500/600.

8. **MEDIUM — R-11 radii off scale (8/6/9/7/4/10px).** `.segmented button` 6px (buttons must be pill), `.topology-node` 9px, `.graph-controls` 7px, `.chart-tooltip` 8px, `.quota-bar` 4px, calendar cells 8px, `.model-id` 6px (sm is legit token), badge-`<b>` 10px. Scale: 6/8/12/9999 only; interactive → pill.

9. **LOW — R-31 endpoint code block is bordered box, not `{component.install-snippet}`.** `.endpoint-value code` has 1px border + radius var; spec: surface-soft pill, no border, `padding 12px 20px`. Same for `.gateway-secret`.

10. **LOW — typography: sub-token sizes.** 9–11px used in ~30 rules (`.usage-kpi small` 9px, `.recent-row time` 9px, chart text 9px, `.connection-meta .badge` 10px). Token floor is `caption-sm` 12px.

11. **LOW — typography: off-scale display sizes.** `.provider-detail-head h2` 25px, `.usage-kpi strong` clamp(19–25px), oauth `.ui-icon` 36px/34px (icon glyph sized like display type). Scale: 36/30/24/20/18/16/14/12.

12. **LOW — `button-secondary` border `#e5e5e5`, spec `{colors.hairline-strong} #d4d4d4`.**

13. **LOW — negative letter-spacing leftovers** (`.brand strong -.025em`, `.provider-detail-head h2 -.025em`, usage strong `-.035em`, summary strong `-.03em`). DESIGN.md: letterSpacing 0 everywhere.

14. **LOW — code font family inconsistency.** `.recent-row strong` / `.detail-drawer dd` use `"SFMono-Regular",Consolas` instead of `ui-monospace` stack.

15. **LOW — `.button:disabled` uses `opacity:.48`; spec `button-disabled` = surface-soft bg + mute text.** Also `button-danger #c4323e` is a second accent (semantic, arguably needed; DESIGN.md has no destructive color — flag for decision).

