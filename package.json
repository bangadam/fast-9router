{
  "name": "fast-9router",
  "private": true,
  "version": "0.1.0",
  "description": "A focused Bun gateway for Codex OAuth, Anthropic, and OpenAI-compatible APIs.",
  "license": "MIT",
  "repository": "github:bangadam/fast-9router",
  "bugs": "https://github.com/bangadam/fast-9router/issues",
  "homepage": "https://github.com/bangadam/fast-9router#readme",
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "start": "bun run src/server.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit --pretty false && tsc -p dashboard/tsconfig.json",
    "bench": "bun scripts/benchmark.ts",
    "build:dashboard": "vite build --config dashboard/vite.config.ts"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/bun": "^1.4.0",
    "preact": "^10.29.8",
    "typescript": "5.9.3",
    "vite": "^8.2.2"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
